

/**
 * @param {import('./interpreter/scope').Scope} scope
 */
function declareNatives(scope) {
	
}

/**
 * @param {import('./interpreter/scope').Scope} scope
 */
function declareContext(scope) {
	
}

module.exports = {
	declareNatives,
	declareContext,
};
